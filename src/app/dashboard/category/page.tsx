import { CategoryPage} from "@/components/pages"

  export const description =
    "An category dashboard with a sidebar navigation. The sidebar has icon navigation. The content area has a breadcrumb and search in the header. It displays a list of products in a table with actions."
    
  export default function Category() {
    return (
        <CategoryPage />
    )
  }